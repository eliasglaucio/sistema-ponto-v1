services:
  app:
    build:
      context: .
    depends_on:
      - compreface
    env_file:
      - .env
    ports:
      - "3000:3000"
    restart: unless-stopped

  compreface:
    image: exadel/compreface:1.2.0
    ports:
      - "8000:8000"
    restart: unless-stopped
